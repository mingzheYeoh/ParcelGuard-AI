/**
 * shadcn/ui 4.x ships components that import `cn` from the official `cn`
 * package (clsx + tailwind-merge, compiled). Re-exported here so application
 * code has a single import site.
 */
export { cn } from 'cn';
